"use client";
import {useState} from "react";
type Result={ok:boolean;offerReference?:string;accessCode?:string;error?:string};
export function OfferHelpForm({requestReference}:{requestReference:string}){const [result,setResult]=useState<Result>();const [busy,setBusy]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const raw=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/offers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...raw,requestReference,etaMinutes:Number(raw.etaMinutes),peopleComing:Number(raw.peopleComing),hasVehicle:raw.hasVehicle==="on",consent:raw.consent==="on"})});setResult(await response.json());setBusy(false)}if(result?.ok)return <section className="notice stack"><h3>Offer sent</h3><p>Offer reference: <strong>{result.offerReference}</strong></p><p>Save this private responder code:</p><p className="access-code">{result.accessCode}</p><p>Use it on the coordination page after the requester accepts your offer.</p></section>;return <form className="card stack" onSubmit={submit}><h2>Offer community help</h2><p className="muted">Your phone number stays hidden until the requester accepts your offer.</p><div className="grid"><label>Name<input name="name" required minLength={2}/></label><label>Mobile<input name="mobile" required inputMode="tel" pattern="\+?[0-9]{10,15}"/></label><label>ETA in minutes<input name="etaMinutes" type="number" min="1" max="1440" required/></label><label>People coming<input name="peopleComing" type="number" min="1" max="100" defaultValue="1" required/></label></div><label>How can you help?<textarea name="helpDescription" required minLength={10}/></label><label><input name="hasVehicle" type="checkbox"/> Vehicle available</label><label><input name="consent" type="checkbox" required/> I consent to share my contact details only if this offer is accepted.</label><label className="honeypot" aria-hidden="true">Website<input name="website" tabIndex={-1}/></label>{result?.error&&<p className="error">Could not send this offer. Please retry.</p>}<button className="button" disabled={busy}>{busy?"Sending…":"Offer help"}</button></form>}
