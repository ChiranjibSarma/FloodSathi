import {createCipheriv,createHmac,randomBytes} from "node:crypto";
export interface SensitiveDataProtector{encrypt(value:string):string;phoneHash(value:string):string}
export class AesGcmSensitiveDataProtector implements SensitiveDataProtector{private readonly key:Buffer;constructor(encryptionKeyBase64:string,private readonly hashKey:string){this.key=Buffer.from(encryptionKeyBase64,"base64");if(this.key.length!==32)throw new Error("Encryption key must contain exactly 32 bytes");if(hashKey.length<32)throw new Error("Phone hash key must contain at least 32 characters")}encrypt(value:string){const iv=randomBytes(12);const cipher=createCipheriv("aes-256-gcm",this.key,iv);const encrypted=Buffer.concat([cipher.update(value,"utf8"),cipher.final()]);return `v1.${iv.toString("base64url")}.${cipher.getAuthTag().toString("base64url")}.${encrypted.toString("base64url")}`}phoneHash(value:string){const normalised=value.replace(/\D/g,"");return createHmac("sha256",this.hashKey).update(normalised).digest("hex")}}
