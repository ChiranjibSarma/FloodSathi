export interface DuplicateFacts{samePhoneHash:boolean;distanceMetres?:number;minutesApart:number;descriptionSimilarity:number;transcriptSimilarity?:number;sameLandmark:boolean;sameVillage:boolean;sameHousehold:boolean}
export interface DuplicateSuggestion{score:number;suggested:boolean;signals:string[];automaticMerge:false}
export function suggestDuplicate(facts:DuplicateFacts):DuplicateSuggestion{let score=0;const signals:string[]=[];const add=(points:number,label:string)=>{score+=points;signals.push(label)};if(facts.samePhoneHash)add(35,"Same protected phone identifier");if(facts.sameHousehold)add(25,"Same household");if(facts.distanceMetres!==undefined&&facts.distanceMetres<=250)add(15,"Locations within 250 metres");if(facts.minutesApart<=180)add(10,"Requests within three hours");if(facts.descriptionSimilarity>=.8)add(10,"Highly similar descriptions");if((facts.transcriptSimilarity??0)>=.8)add(10,"Highly similar voice transcripts");if(facts.sameLandmark)add(5,"Same landmark");if(facts.sameVillage)add(5,"Same village");return {score:Math.min(score,100),suggested:score>=50,signals,automaticMerge:false}}
