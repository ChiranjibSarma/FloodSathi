export type Priority="P0_CRITICAL"|"P1_HIGH"|"P2_ESSENTIAL"|"P3_RECOVERY";
export interface TriageFacts{immediateDanger:boolean;medicalEmergency:boolean;evacuationRequired:boolean;peopleTrapped?:boolean;drowningRisk?:boolean;seriousInjury?:boolean;pregnantInDistress?:boolean;oxygenDependent?:boolean;buildingCollapse?:boolean;vulnerableStranded?:boolean;affectedPeople:number;children:number;elderly:number;hoursWithoutEssentials?:number;recoveryOnly?:boolean}
export interface PrioritySuggestion{suggestedPriority:Priority;reasons:string[];requiresCoordinatorConfirmation:true;mayAutoDispatch:false}
export function suggestPriority(facts:TriageFacts):PrioritySuggestion{const critical:[boolean,string][]=[[facts.peopleTrapped===true,"People reported trapped"],[facts.drowningRisk===true,"Drowning risk reported"],[facts.medicalEmergency,"Medical emergency reported"],[facts.seriousInjury===true,"Serious injury reported"],[facts.pregnantInDistress===true,"Pregnant person reported in distress"],[facts.oxygenDependent===true,"Oxygen-dependent person reported"],[facts.buildingCollapse===true,"Building collapse reported"],[facts.vulnerableStranded===true,"Vulnerable people stranded in immediate danger"],[facts.immediateDanger,"Immediate danger reported"]];const reasons=critical.filter(([active])=>active).map(([,reason])=>reason);if(reasons.length)return {suggestedPriority:"P0_CRITICAL",reasons,requiresCoordinatorConfirmation:true,mayAutoDispatch:false};if(facts.evacuationRequired||facts.children>0||facts.elderly>0)return {suggestedPriority:"P1_HIGH",reasons:[facts.evacuationRequired?"Evacuation requested":"Vulnerable people affected"],requiresCoordinatorConfirmation:true,mayAutoDispatch:false};if(facts.recoveryOnly)return {suggestedPriority:"P3_RECOVERY",reasons:["Reported need is recovery-stage"],requiresCoordinatorConfirmation:true,mayAutoDispatch:false};return {suggestedPriority:"P2_ESSENTIAL",reasons:[facts.hoursWithoutEssentials&&facts.hoursWithoutEssentials>=24?"Essential supplies unavailable for 24 hours or more":"Essential assistance requested"],requiresCoordinatorConfirmation:true,mayAutoDispatch:false}}
