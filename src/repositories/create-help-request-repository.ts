import "server-only";import {createClient} from "@supabase/supabase-js";import {serverEnv} from "@/lib/env";import {AesGcmSensitiveDataProtector} from "@/services/security/sensitive-data";import {PersistenceUnavailableError,type HelpRequestRepository} from "./help-request-repository";import {SupabaseHelpRequestRepository} from "./supabase-help-request-repository";
export function createHelpRequestRepository():HelpRequestRepository{const env=serverEnv();if(!env.NEXT_PUBLIC_SUPABASE_URL||!env.SUPABASE_SERVICE_ROLE_KEY||!env.DATA_ENCRYPTION_KEY||!env.PHONE_HASH_KEY)throw new PersistenceUnavailableError();const db=createClient(env.NEXT_PUBLIC_SUPABASE_URL,env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});return new SupabaseHelpRequestRepository(db,new AesGcmSensitiveDataProtector(env.DATA_ENCRYPTION_KEY,env.PHONE_HASH_KEY))}
