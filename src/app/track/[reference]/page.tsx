import {notFound} from "next/navigation";export default async function Track({params}:{params:Promise<{reference:string}>}){const {reference}=await params;if(!/^(FSA-[A-Z0-9]{8,24}|demo)$/i.test(reference))notFound();return <div className="container stack"><h1>আবেদন অনুসৰণ · Track request</h1><section className="card stack"><p>Public reference</p><h2>{reference}</h2><p><span className="status">NEW · Received</span></p><p>Last update: awaiting coordinator review</p><p className="muted">No exact location, phone number, medical details, volunteer identity, evidence, or internal notes are shown here.</p><div className="actions"><button className="button">Report situation worsened</button><button className="button secondary">Cancel request</button></div></section><aside className="notice">FloodSathi Assam is not a replacement for official emergency services.</aside></div>}
