import {describe,expect,it} from "vitest";import {assertTransition,canTransition,createPublicReference,helpRequestSchema} from "../src/domain/request";
describe("request domain",()=>{const valid={language:"as-IN",category:"FOOD",name:"Demo Person",mobile:"9999999999",district:"Demo District",locality:"Demo Village",affectedPeople:2,children:1,elderly:0,medicalEmergency:false,immediateDanger:false,evacuationRequired:false,description:"পৰীক্ষাৰ বাবে কাল্পনিক সহায় আবেদন",consent:true,idempotencyKey:crypto.randomUUID()};it("accepts a valid Assamese request",()=>{expect(helpRequestSchema.safeParse(valid).success).toBe(true)});it("accepts consented coordinates",()=>expect(helpRequestSchema.safeParse({...valid,latitude:26.14,longitude:91.74,locationSource:"GPS",locationConsent:true}).success).toBe(true));it("rejects exact coordinates without location consent",()=>expect(helpRequestSchema.safeParse({...valid,latitude:26.14,longitude:91.74,locationSource:"GPS",locationConsent:false}).success).toBe(false));it("rejects incomplete coordinate pairs",()=>expect(helpRequestSchema.safeParse({...valid,latitude:26.14,locationSource:"GPS",locationConsent:true}).success).toBe(false));it("rejects invalid status transitions",()=>{expect(canTransition("NEW","RESOLVED")).toBe(false);expect(()=>assertTransition("NEW","RESOLVED")).toThrow(/Invalid/)});it("allows governed status transitions",()=>expect(canTransition("VERIFICATION_PENDING","RESOLVED")).toBe(true));it("creates opaque references",()=>{const a=createPublicReference(),b=createPublicReference();expect(a).toMatch(/^FSA-[A-Z0-9]+$/);expect(a).not.toBe(b)})});
